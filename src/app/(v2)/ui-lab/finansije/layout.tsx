// Finansije — hub + sub-modules
// Font + token setup is in the parent (v2)/ui-lab/layout.tsx

export const metadata = {
  title: "Finansije | UI Lab",
  robots: "noindex, nofollow",
};

export default function FinansijeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
